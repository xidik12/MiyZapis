
# Capacitor native projects ARE committed (so Info.plist perms, push entitlements and
# other native config persist). Build artifacts + the synced web bundle are excluded by
# ios/.gitignore and android/.gitignore.
